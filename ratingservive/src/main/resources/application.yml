
spring:
  data:
    mongodb:
      uri: mongodb://localhost:27017
      database: ratingservice
server:
  port: 8083

eureka:
  instance:
    prefer-ip-address: true
  client:
    fetch-registry: true
    register-with-eureka: true